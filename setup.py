from setuptools import find_packages, setup

setup(
    name = 'Generative AI Project-Llama on CPU',
    version= '0.0.0',
    author= 'Eric',
    author_email= 'impactmylife1@gmail.com',
    packages= find_packages(), # the find_packages will look for the constructorfile (--init__.py under the src folder)
    install_requires = []

)